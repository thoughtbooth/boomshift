<h1>My Business</h1>
<div class="panel panel-default">
  <div class="panel-body">
    <% @biz = current_user.business %>
    <% if @biz %>
      <% @bizname = @biz.name %>
      <p><%= link_to 'My Business Profile', business_path(@biz) %> (<%= @bizname %>)</p>
    <% else %>
      <p><%= link_to 'Create My Business Profile', new_business_path %></p>
    <% end %>
  </div>
</div>