<%= simple_form_for(@payment_term) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :days_to_pay %>
    <%= f.hidden_field :business_id, value: current_user.business.id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
