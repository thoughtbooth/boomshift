<%= simple_form_for @invoicing_line_item do |f| %>
  <!--%= f.hidden_field :ledger_item_id, value: params[:ledger_item_id] %-->
  <%= f.association :job, collection: Job.completed_or_billed, label_method: :description, value_method: :id, prompt: "Choose the job you want to bill for" %>
  <%= f.input :description %>
  <!--%= f.hidden_field :net_amount, value: enrollment.service_price %-->
  <!--%= f.hidden_field :quantity, value: job.hours_worked %-->
  <%= f.hidden_field :creator_id, value: current_user.id %> <!-- Set this in the controller instead. -->
  <%= f.button :button, class: 'button btn-default' do %>
    <i class="fa fa-floppy-o"></i> Save
  <% end %>
<% end %>