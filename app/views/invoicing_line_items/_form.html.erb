<%= simple_form_for @invoicing_line_item do |f| %>
  <%= f.input :ledger_item_id %> <!-- Change this to a hidden field after I get it to populate with the correct invoice id -->
  <!--%= f.hidden_field :currency, value: "usd" %-->
  <%= f.input :type %> <!-- Is this used for subclassing? Do I need this? -->
  $<!--%= f.input :net_amount, value: number_with_precision(f.object.net_amount, precision: 2) %-->
  <%= f.input :description %>
  <%= f.input :quantity %>
  <%= f.input :creator_id %> <!-- Change this to a hidden field after I get it to populate with the correct user id -->
<%= f.association :job, collection: Job.completed_or_invoiced, label_method: :description, value_method: :id, prompt: "Choose the job you want to bill for" %>
  <!--%= f.association :recipient, label_method: :full_name, value_method: :id, prompt: "Choose the recipient (client)" %-->


  <%= f.button :button, class: 'button btn-default' do %>
    <i class="glyphicon glyphicon-floppy-disk"></i> Save
  <% end %>
<% end %>